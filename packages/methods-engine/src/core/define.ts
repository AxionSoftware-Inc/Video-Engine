import type { ExampleSpec, MethodSpec } from "./types";

export function defineMethod(method: MethodSpec): MethodSpec {
  return method;
}

export function defineExample(example: ExampleSpec): ExampleSpec {
  return example;
}
